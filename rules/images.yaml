# Put source image on the left, destination image on the right. 
# For example:
#
#    // This rule means sync all images in left repo to right repo
#    "quay.io/kubernetes-multicluster/kubefed": "kubespheredev/kubesphere"  
#
#    // Only sync image tagged v0.3.0 to right repo
#    "quay.io/kubernetes-multicluster/kubefed:v0.3.0": "kubespheredev/kubesphere"  
#
#    // Sync multiple images to right repo
#    "quay.io/kubernetes-multicluster/kubefed:v0.3.0,v0.4.0,v0.5.0": "kubespheredev/kubesphere"  
#
# If images are not public, please contact @zryfish to add registry credentials.
#
#
# Testing
# For testing purpose
#
"quay.io/kubernetes-multicluster/kubefed:v0.3.0": "kubespheredev/kubesphere"

# Development
# This section are rules used to sync images that are still under development

# Release
# This section defines rules which used to sync images when issuing a release.

